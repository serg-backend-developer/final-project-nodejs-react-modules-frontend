import BeatLoader from "react-spinners/BeatLoader";

const Loader = () => {
    return (
            <BeatLoader
                aria-label="Loading Spinner"
                data-testid="loader"
        />
    )
};

export default Loader;